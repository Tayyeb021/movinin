{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": ["cd admin && npm ci"]
    },
    "build": {
      "commands": ["cd admin && npm run build"]
    }
  },
  "deploy": {
    "inputs": [
      "...",
      { "step": "build", "include": ["admin/build"] }
    ]
  }
}
